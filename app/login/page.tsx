import LoginLocal from "@/components/local/login_local";
import React from "react";

function LoginPage() {
    return (
        <div>
            <LoginLocal />
        </div>
    );
}

export default LoginPage;
